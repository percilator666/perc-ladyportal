config = {
    framework = 'qbx', -- 'qbx' | 'esx' | 'qb'
    notification = 'ox', -- 'ox' | 'custom' [client/customize.lua]
    progress = 'oxbar', -- 'oxbar' | 'oxcircle' | 'custom' [client/customize.lua]
    
    serverinfo = {
        name = 'Server Name',
        discord = 'discord.gg/servervanity',
        webhook = {
            redeemedjob = 'webhook_link',
            redeemeditems = 'webhook_link',
            redeemedvehicle = 'webhook_link'
        }
    }
}
